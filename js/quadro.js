(function ($) {
  $(document).ready(function() {
    // Find the page title and add the fitText plugin

  $('#page-title-wrapper').bigtext({
    maxfontsize: 48, // default is 48 (in px)
    childSelector: '> h1'
});


  });
}(jQuery));


